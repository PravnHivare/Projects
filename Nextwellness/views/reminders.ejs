<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reminders</title>
</head>
<body>
    <h1>Reminders</h1>
    <% if (reminders.length > 0) { %>
        <ul>
            <% reminders.forEach(reminder => { %>
                <li>User: <%= reminder.userName %>, Appointment: <%= reminder.appointmentName %>, Date: <%= reminder.appointmentDate %>, Time: <%= reminder.appointmentTime %></li>
            <% }); %>
        </ul>
    <% } else { %>
        <p>No reminders to display.</p>
    <% } %>
</body>
</html>
